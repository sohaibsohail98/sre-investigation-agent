# Terraform
terraform/.terraform/
terraform/*.tfstate
terraform/*.tfstate.*
terraform/tfplan*
terraform/*.tfplan
terraform/crash.log

# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/

# Local execution recorder DB — regenerated by running the agent locally
data/metrics.db

# Local env / secrets
.env
.env.*
!.env.example
*.pem

# Session/handoff working notes — not part of the shipped project.
PLAN.md
