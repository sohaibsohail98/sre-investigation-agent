# Local dev/testing only — not what gets deployed (see agent/requirements.txt
# for the minimal set that actually ships in the AgentCore container).
bedrock-agentcore
boto3
gradio
mcp
starlette
uvicorn
pytest
playwright
# mcp_server/ + metrics/ live here now — pinned to a specific commit;
# bump the SHA deliberately, don't float on @main.
git+https://github.com/sohaibsohail98/mcp-context-inspector@17e22ee6bcd361001b9e0a883302e37665726457
