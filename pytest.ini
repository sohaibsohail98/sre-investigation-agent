[pytest]
testpaths = tests
# tests/run_eval.py is a separate, deliberately live-Bedrock-cost eval
# harness (`uv run python -m tests.run_eval`), not a pytest file — it
# doesn't match test_*.py, so pytest's default collection already skips
# it; this is just making that explicit rather than accidental.
python_files = test_*.py
